---
id: List
title:  Catálogo (List)
sidebar_position: 1
---

# Componente List

El componente `<List />` actúa como el **contenedor principal** del catálogo. Su función es recibir un inventario de datos y orquestar la renderización de tarjetas de producto (`ProductCard`) en una cuadrícula responsiva.

:::info Propósito
Este componente no realiza llamadas a APIs, simplemente visualiza los datos que recibe por props, asegurando que el diseño se rompa elegantemente según el tamaño de pantalla.
:::

## ♿ Accesibilidad y Semántica

Este componente implementa:
* **Semántica:** Utiliza una lista desordenada `<ul>` con el rol `role="list"` para que los lectores de pantalla anuncien correctamente el número de ítems.
* **Navegación:** Cada tarjeta contiene enlaces semánticos que permiten la navegación vía teclado (`Tab`).
* **Imágenes:** Gestiona los atributos `alt` de forma dinámica basándose en el nombre del producto.

---

## 🎮 Demo Interactiva

A continuación se muestra una instancia viva del componente cargada desde **Storybook**.

<div style={{border: '1px solid #e5e7eb', borderRadius: '8px', overflow: 'hidden', marginBottom: '2rem'}}>
  <iframe
    src="http://localhost:6006/iframe.html?args=&id=components-list--default&viewMode=story"
    width="100%"
    height="500px"
    style={{border: 'none'}}
    title="Catálogo de Frutas"
  ></iframe>
</div>

---

## 🛠️ API de Componente

### Propiedades (Props)

| Propiedad | Tipo | Requerido | Descripción |
| :--- | :--- | :--- | :--- |
| **`items`** | `Producto[]` | ✅ Sí | Array de objetos que contiene la información a renderizar. |

### Definición de Tipo: `Producto`

El objeto esperado debe cumplir la siguiente interfaz:

```ts
interface Producto {
  id: number | string;      // Identificador único (clave de React)
  nombre: string;           // Título accesible del producto
  precio: number | string;  // Valor monetario
  imagen: string;           // Ruta relativa (public) o absoluta
}